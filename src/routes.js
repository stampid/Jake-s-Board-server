// user
const USER = "/user";
const EMAILSEND = "/emailsend";
const EMAILAUTH = "/emailauth";
const SIGNUP = "/signup";
const SIGNIN = "/signin";
const SIGNOUT = "/signout";

const routes = {
  user: USER,
  emailSend: EMAILSEND,
  emailAuth: EMAILAUTH,
  signUp: SIGNUP,
  signIn: SIGNIN,
  signOut: SIGNOUT
};

export default routes;
