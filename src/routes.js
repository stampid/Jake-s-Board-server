// user
const USER = "/user";
const EMAILSEND = "/emailsend";
const EMAILAUTH = "/emailauth";
const SIGNUP = "/signup";

const routes = {
  user: USER,
  emailSend: EMAILSEND,
  emailAuth: EMAILAUTH,
  signUp: SIGNUP
};

export default routes;
